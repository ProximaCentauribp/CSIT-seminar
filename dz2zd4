#include <iostream>
#include <cmath>

using namespace std;

int main() {
	setlocale(LC_ALL, "RUS");
	int x, a, b;
	cout << "Введите число: ";
	cin >> x;
	a = x / 10;
	b = x % 10;
	for (int i = (a + b); i <= (a * b); i++)
		cout << i << endl;
	return 0;
}
