#include <iostream>
#include <cmath>

using namespace std;

int main() {
	setlocale(LC_ALL, "RUS");
	int a, b, c;
	cout << "Введите стороны треугольника: ";
	cin >> a >> b >> c;
	double p = (a + b + c) / 2.;
	cout << "Его площадь равна: " << sqrt(p * (p - a) * (p - b) * (p - c));
	return 0;
}
